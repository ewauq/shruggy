module.exports = function(bot) {

    /**
     * Est exécuté chaque fois qu'un utilisateur change de statut.
     */
    bot.on("presence", function(user, userID, status, game, event) {

        // code

    });

};
